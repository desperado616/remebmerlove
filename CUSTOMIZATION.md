# 🎨 Кастомизация Love Reminder Bot

Полное руководство по настройке бота под себя!

## 📝 Содержание

1. [Изменение сообщений](#изменение-сообщений)
2. [Настройка расписания](#настройка-расписания)
3. [Добавление эмодзи](#добавление-эмодзи)
4. [Отправка фото](#отправка-фото)
5. [Специальные даты](#специальные-даты)
6. [Продвинутые настройки](#продвинутые-настройки)

---

## 📝 Изменение сообщений

### Добавить новое сообщение

Откройте `reminder.yml` и добавьте строку в массив `MESSAGES`:

```bash
"💖✨ Ваше новое сообщение здесь! 🌟💕"
```

### Формат сообщения

Рекомендуемая структура:
```
[эмодзи в начале] Текст сообщения [эмодзи в конце]
```

Примеры:
```bash
"💖🌸✨ Доброе утро, солнышко! Люблю тебя! 🥰💕🌟"
"🌙💫💗 Спокойной ночи, моя любимая! Сладких снов! 😴💖✨"
"☕💕🌈 Хорошего дня, котёнок! Ты лучшая! 🐱💖🌟"
```

### Категории сообщений

Создайте разные категории для разного времени суток:

**Утренние (07:00 UTC)**
```bash
"☀️🌅💖 Доброе утро, любимая! Пусть день будет чудесным! 🌸💕✨"
"🌞💗🦋 Просыпайся, солнышко! Новый день полон возможностей! 💖🌟🌈"
```

**Дневные (13:00 UTC)**
```bash
"☕💕🌸 Как дела, котёнок? Не забывай пообедать! 🍽️💖✨"
"🌤️💗🦄 Надеюсь, у тебя всё отлично! Думаю о тебе! 💭💖🌟"
```

**Вечерние (18:00 UTC)**
```bash
"🌆💕🌸 Как прошёл день, любимая? Скучаю! 💖✨🥰"
"🌙💗⭐ Вечер — время подумать о тебе! Люблю! 💖🌟💕"
```

---

## ⏰ Настройка расписания

### Изменить время отправки

Формат cron: `минута час день месяц день_недели`

```yaml
- cron: '0 7 * * *'   # Каждый день в 07:00 UTC
- cron: '30 12 * * *' # Каждый день в 12:30 UTC
- cron: '0 18 * * 1'  # Каждый понедельник в 18:00 UTC
```

### Конвертация времени

Для UTC+5 (например, Ташкент):
- Желаемое время - 5 часов = UTC время
- 12:00 UTC+5 → 07:00 UTC
- 18:00 UTC+5 → 13:00 UTC
- 23:00 UTC+5 → 18:00 UTC

### Примеры расписаний

**Каждые 2 часа (с 8:00 до 22:00 UTC+5)**
```yaml
- cron: '0 3 * * *'   # 08:00 UTC+5
- cron: '0 5 * * *'   # 10:00 UTC+5
- cron: '0 7 * * *'   # 12:00 UTC+5
- cron: '0 9 * * *'   # 14:00 UTC+5
- cron: '0 11 * * *'  # 16:00 UTC+5
- cron: '0 13 * * *'  # 18:00 UTC+5
- cron: '0 15 * * *'  # 20:00 UTC+5
- cron: '0 17 * * *'  # 22:00 UTC+5
```

**Только по будням**
```yaml
- cron: '0 7 * * 1-5'  # Понедельник-Пятница
```

**Только выходные**
```yaml
- cron: '0 7 * * 0,6'  # Воскресенье и Суббота
```

---

## 🎨 Добавление эмодзи

### Популярные эмодзи для любовных сообщений

**Сердца**
```
❤️ 💖 💗 💓 💕 💞 💘 💝 💟 ❣️
```

**Лица**
```
😍 🥰 😘 😊 🤗 😌 🥺 😽 😻 🤩
```

**Природа**
```
🌸 🌺 🌷 🌹 🌻 🌼 💐 🌈 ⭐ ✨
```

**Животные**
```
🐰 🐱 🐻 🦋 🐾 🦄 🐇 🐈 🧸
```

**Символы**
```
💫 ⚡ 🔥 💎 👑 🎀 🎁 🎉 🎊 🎈
```

### Комбинации эмодзи

```bash
# Утро
"☀️🌅💖 ... 🌸💕✨"

# День
"🌤️💗🦋 ... 💖🌟🌈"

# Вечер
"🌆💕🌸 ... 💖✨🥰"

# Ночь
"🌙💫💗 ... 😴💖✨"
```

---

## 📸 Отправка фото

### Отправить фото с подписью

Создайте новый файл `.github/workflows/photo-reminder.yml`:

```yaml
name: 💝 Photo Love Message

on:
  schedule:
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  send-photo:
    runs-on: ubuntu-latest
    steps:
    - name: Send photo with caption
      run: |
        CAPTIONS=(
          "💖 Ты моё всё! 💕"
          "🥰 Люблю тебя! 💗"
          "😘 Скучаю! 💖"
        )
        CAPTION="${CAPTIONS[$RANDOM % ${#CAPTIONS[@]}]}"
        
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendPhoto" \
          -F chat_id="${{ secrets.CHAT_ID }}" \
          -F photo="URL_ВАШЕГО_ФОТО" \
          -F caption="$CAPTION"
```

### Отправить стикер

```yaml
- name: Send sticker
  run: |
    curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendSticker" \
      -d chat_id="${{ secrets.CHAT_ID }}" \
      -d sticker="FILE_ID_СТИКЕРА"
```

---

## 🎂 Специальные даты

### Сообщения для особых дней

Создайте файл `.github/workflows/special-dates.yml`:

```yaml
name: 🎉 Special Date Reminder

on:
  schedule:
    # 14 февраля в 10:00 UTC+5
    - cron: '0 5 14 2 *'
    # День рождения (например, 15 марта)
    - cron: '0 5 15 3 *'
    # Годовщина (например, 20 июня)
    - cron: '0 5 20 6 *'
  workflow_dispatch:

jobs:
  send-special-message:
    runs-on: ubuntu-latest
    steps:
    - name: 💝 Send special message
      run: |
        MONTH=$(date +%m)
        DAY=$(date +%d)
        
        if [ "$MONTH" = "02" ] && [ "$DAY" = "14" ]; then
          MSG="💖🌹✨ С Днём Святого Валентина, моя любимая! Ты моё всё! 💕🎁🌟"
        elif [ "$MONTH" = "03" ] && [ "$DAY" = "15" ]; then
          MSG="🎂🎉💖 С Днём Рождения, солнышко! Желаю всего самого лучшего! 🎁💕✨"
        elif [ "$MONTH" = "06" ] && [ "$DAY" = "20" ]; then
          MSG="💑💖✨ С годовщиной, любимая! Спасибо за эти чудесные дни вместе! 💕🌹🎉"
        else
          MSG="💖✨ Особенный день для особенной девушки! 💕🌟"
        fi
        
        curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
          -d chat_id="${{ secrets.CHAT_ID }}" \
          -d text="$MSG"
```

---

## 🚀 Продвинутые настройки

### Случайное время отправки

Добавьте вариативность во время:

```yaml
- name: Send at random time
  run: |
    # Случайная задержка 0-30 минут
    DELAY=$((RANDOM % 1800))
    sleep $DELAY
    
    # Отправка сообщения
    curl -s -X POST ...
```

### Отправка нескольких сообщений

```yaml
- name: Send multiple messages
  run: |
    MESSAGES=(
      "💖 Первое сообщение! 💕"
      "🥰 Второе сообщение! 💗"
      "😘 Третье сообщение! 💖"
    )
    
    for MSG in "${MESSAGES[@]}"; do
      curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
        -d chat_id="${{ secrets.CHAT_ID }}" \
        -d text="$MSG"
      sleep 2  # Пауза 2 секунды между сообщениями
    done
```

### Форматирование текста

Используйте HTML или Markdown:

```yaml
# HTML
-d text="<b>Жирный текст</b> <i>Курсив</i> <u>Подчёркнутый</u>" \
-d parse_mode="HTML"

# Markdown
-d text="*Жирный* _Курсив_ [Ссылка](https://example.com)" \
-d parse_mode="Markdown"
```

### Отправка голосовых сообщений

```yaml
- name: Send voice message
  run: |
    curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendVoice" \
      -F chat_id="${{ secrets.CHAT_ID }}" \
      -F voice="@path/to/voice.ogg" \
      -F caption="💖 Голосовое сообщение для тебя! 💕"
```

---

## 💡 Советы и трюки

### 1. Используйте переменные

```yaml
env:
  MORNING_TIME: '0 7 * * *'
  EVENING_TIME: '0 18 * * *'
```

### 2. Добавьте условия

```yaml
- name: Weekend special
  if: github.event.schedule == '0 7 * * 0,6'
  run: |
    MSG="🎉 Выходные! Давай проведём их вместе! 💖"
```

### 3. Логирование

```yaml
- name: Log message
  run: |
    echo "Отправлено: $MSG"
    echo "Время: $(date)"
```

### 4. Уведомления об ошибках

```yaml
- name: Notify on failure
  if: failure()
  run: |
    curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
      -d chat_id="${{ secrets.ADMIN_CHAT_ID }}" \
      -d text="⚠️ Ошибка в боте!"
```

---

## 🎯 Готовые шаблоны

### Минималистичный стиль

```bash
"Люблю тебя ❤️"
"Думаю о тебе 💭"
"Скучаю 🥺"
"Ты лучшая 💖"
```

### Романтичный стиль

```bash
"💖✨ Ты моё солнце, моя луна, моя вселенная! 🌟💕"
"🌹💗 Каждый день с тобой — это подарок судьбы! 💖✨"
```

### Игривый стиль

```bash
"😽💕 Мяу! Котёнок скучает по тебе! 🐱💖"
"🐰💗 Зайка, где ты? Хочу обнимашек! 🤗💕"
```

---

## 📚 Дополнительные ресурсы

- [Emoji Cheat Sheet](https://www.webfx.com/tools/emoji-cheat-sheet/)
- [Cron Generator](https://crontab.guru/)
- [Telegram Bot API](https://core.telegram.org/bots/api)

---

**Создавайте свои уникальные сообщения!** 💖✨
